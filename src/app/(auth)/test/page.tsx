'use client'
import React from 'react';
const Test = () => {
    const client = 0;
    if (client) return (
      <div>client</div>
    );
    else return (
      <div>

      </div>
    );
}

export default Test;